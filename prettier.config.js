/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  printWidth: 150,
  semi: false,
  singleQuote: true,
  endOfLine: 'auto',
  arrowParens: 'avoid'
};

export default config;